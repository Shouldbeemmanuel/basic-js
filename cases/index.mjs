let name = "emmanuel";
console.log('hello $(name $(1 + 1)');

/*
${ } is used to perform js operation
    such as +, -, *, / variable call, function call
    ${} gives only one value
        it means $(name) possible
        but  $(name, surname) not possible
*/ 